using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    [Tooltip("Relative path from project root or absolute path to an Excel file (.xls/.xlsx). Example: Assets/Data/test.xlsx")]
    public string excelFilePath = "Assets/Data/test.xlsx";

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
